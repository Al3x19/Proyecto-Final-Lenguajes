meta {
  name: POST Publisher
  type: http
  seq: 1
}

post {
  url: {{PATH}}/publishers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "name": "string",
    "description": "stringstri"
  }
}
